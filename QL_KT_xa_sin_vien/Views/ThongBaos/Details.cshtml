@model QL_KT_xa_sin_vien.Models.ThongBao

@{
    ViewData["Title"] = "Chi tiết thông báo";
}

<h2 class="mb-4">Chi tiết thông báo</h2>

<div class="card shadow-sm">
    <div class="card-body">
        <table class="table table-striped table-bordered">
            <tr>
                <th>Loại thông báo</th>
                <td>@Model.LoaiThongBao</td>
            </tr>
            <tr>
                <th>Nội dung</th>
                <td>@Model.NoiDung</td>
            </tr>
            <tr>
                <th>Trạng thái</th>
                <td>
                    @if (Model.TrangThai == "Đã gửi")
                    {
                        <span class="badge bg-success">Đã gửi</span>
                    }
                    else if (Model.TrangThai == "Chưa gửi")
                    {
                        <span class="badge bg-warning text-dark">Chưa gửi</span>
                    }
                    else
                    {
                        <span class="badge bg-secondary">@Model.TrangThai</span>
                    }
                </td>
            </tr>
            <tr>
                <th>Thời gian gửi</th>
                <td>@Model.ThoiGianGui</td>
            </tr>
            <tr>
                <th>Người nhận</th>
                <td>@Model.NguoiNhanNavigation?.MaTaiKhoan</td>
            </tr>
        </table>

        <div class="mt-3">
            <a asp-action="Edit" asp-route-id="@Model?.MaThongBao" class="btn btn-warning me-2">Chỉnh sửa</a>
            <a asp-action="Index" class="btn btn-secondary">Quay lại danh sách</a>
        </div>
    </div>
</div>
